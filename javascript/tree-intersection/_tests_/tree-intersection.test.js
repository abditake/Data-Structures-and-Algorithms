const treeIntersection = require('../index');




describe('Tree-collision tests', () => {
  test('should return a list of values that are intersecting', () => {
    // check the array the is outputted?

    expect().toEqual();
  });
});


